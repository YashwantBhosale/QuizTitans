MONGO_URI=mongodb+srv://shweta_jadhav:?H:EwAggj3Sq&7=@quizmaker.52uwu.mongodb.net/?retryWrites=true&w=majority&appName=QuizMaker

in .env file this was giving error as it contains special characters like =, &, @ etc. 
that's why 
    ? → %3F
    : → %3A
    & → %26
    = → %3D      
Use an online URL encoder or manually replace the special characters with their encoded values: